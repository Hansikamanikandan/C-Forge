#include <stdio.h>
int main() {
    float m1,m2,m3,m4,m5,total,per;
    scanf("%f %f %f %f %f",&m1,&m2,&m3,&m4,&m5);

    total=m1+m2+m3+m4+m5;
    per=total/5;

    printf("Total=%.2f Percentage=%.2f",total,per);
    return 0;
}